#version 450
#extension GL_GOOGLE_include_directive : require
#extension GL_EXT_control_flow_attributes : require
#include "evolution_pass.inc"
void main(){dw_stage_operators();uint local=gl_GlobalInvocationID.x,i=cfg.reserved0+local;if(i>=cfg.count)return;EvolutionScratch item=work[local];Motion value=dw_slope(stateIn[i],cfg,item,cfg.reserved1);if(cfg.reserved1==0u)item.k1=value;else if(cfg.reserved1==1u)item.k2=value;else if(cfg.reserved1==2u)item.k3=value;else item.k4=value;work[local]=item;}
